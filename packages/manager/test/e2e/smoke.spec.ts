import { test } from "@playwright/test";

test.describe("okastr8 e2e smoke", () => {
    test.skip("requires a running manager and configured auth", async () => {
        // Phase 0 scaffold: implemented in later phases.
    });
});
